# OSOP Session Logging Configuration for SWE-agent
#
# Add this to your SWE-agent config or reference via --config flag.
# After completing multi-step tasks, the agent will produce
# .osop workflow definitions and .osoplog.yaml execution records.

agent:
  system_template: |
    After completing a significant task (multi-step work, not trivial one-liners),
    produce an OSOP session log:

    1. Create a `.osop` file describing the workflow you followed
    2. Create a `.osoplog.yaml` file recording what you actually did

    .osop format:
    ```yaml
    osop_version: "1.0"
    id: "session-<short-description>"
    name: "<What you did>"
    description: "<1-2 sentence summary>"
    version: "1.0.0"
    tags: [swe-agent, <relevant-tags>]
    nodes:
      - id: "<step-id>"
        type: "<node-type>"  # human, agent, mcp, cli, api, cicd, git
        name: "<Step Name>"
        description: "<What this step does>"
    edges:
      - from: "<step-a>"
        to: "<step-b>"
        mode: "sequential"
    ```

    .osoplog.yaml format:
    ```yaml
    osoplog_version: "1.0"
    run_id: "<uuid>"
    workflow_id: "<matches .osop id>"
    mode: "live"
    status: "COMPLETED"
    started_at: "<ISO timestamp>"
    ended_at: "<ISO timestamp>"
    duration_ms: <total ms>
    runtime:
      agent: "swe-agent"
      model: "<model>"
    node_records:
      - node_id: "<step-id>"
        node_type: "<type>"
        status: "COMPLETED"
        duration_ms: <ms>
        tools_used:
          - { tool: "<tool-name>", calls: <n> }
    result_summary: "<1-2 sentence summary>"
    ```

    Save to sessions/ directory.
